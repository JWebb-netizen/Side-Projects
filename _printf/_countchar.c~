#include "main.h"

int _countchar(char c)
{
	int count = 0;
	
	for (count = 0; c != '\0'; c++)
	{
		if (c == % && c + 1 == %)
		{
			c++;
			count++;
		}
		else if (c == %)
		{
			c++;
		}
		else
		{
			i++;
		}
	}
	return (i);
}
